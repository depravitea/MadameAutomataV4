import { SlashCommandBuilder } from 'discord.js'; export const data=new SlashCommandBuilder().setName('safeword').setDescription('Use the server safeword to clear active punishments'); export async function execute(i){ const word=process.env.SAFEWORD||'mercy'; await i.reply({ content:`Safeword acknowledged: **${word}**. Moderators alerted; active punishments will be reviewed.`, ephemeral:true}); }
